---
adj_data:
  sensor_ids_file: 307  # 节点个数，或者节点编号文件
  distance_file: dataset/PEMS04/distance.csv
  normalized_k: 0.1
  output_pkl_file: null  # null表示默认路径

traffic_data:
  data_file: dataset/PEMS04/pems04.npz
  data_file_type: npz  # 数据集文件类型，后缀，["npz", "h5"]
  add_time_in_day: true
  add_day_in_week: false
  output_npz_file: null  # null表示默认路径
  split_list:  # 划分比例：训练集、验证集、测试集
    - 6
    - 2
    - 2